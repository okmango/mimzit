import SwiftUI
import SwiftData

/// Review playback screen — assembled in Task 2.
/// This stub satisfies the Xcode project reference while Task 1 builds.
struct ReviewView: View {
    let session: Session

    var body: some View {
        Text("Review")
    }
}
